interactable plots:
1. scatter plot - `screen_time` vs `sleep_quality`, dot color = `stress_level`/`happiness_index` (can be toggled at the top)
2. bubble grid chart - `exercise_freqency` vs `days_without_social_media`, size = count, color = avg of `stress_level`/`happiness_index` (can be toggled at the top)
3. bar chart for `age`, `daily_screen_time`, `sleep_quality`, `stress_level`, `dats_without_social_media`, `exercise_frequency`, `happiness_index`, with a toggle for `(none)`/`gender`/`social_media_platform`

interaction: brushing-and-linking system, tooltip

[web site](https://shadowhunter2155.github.io/Data-Visualization-Project/)
