current thoughts for interactable plots:
1. scatter plot - `screen_time` vs `sleep_quality`, dot color = `stress_level`/`happiness_index` (can be toggled at the color bar legend)
2. bubble grid chart - `exercise_freqency` vs `days_without_social_media`, size = count, color = avg of `stress_level`/`happiness_index` (can be toggled at the color bar legend
   - Hovering a bubble shows distribution of `stress_level`/`happiness_index` inside that bin
3. bar chart for `age`, `daily_screen_time`, `sleep_quality`, `stress_level`, `dats_without_social_media`, `exercise_frequency`, `happiness_index`, with a toggle for `(none)`/`gender`/`social_media_platform`

interaction: brush
